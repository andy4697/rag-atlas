# Essential packages for Airflow DAGs
requests>=2.31.0
httpx>=0.25.0

# Airflow providers (these should be installed via pip install apache-airflow[postgres,redis,http])
# apache-airflow-providers-postgres>=5.7.0
# apache-airflow-providers-redis>=3.4.0  
# apache-airflow-providers-http>=4.7.0